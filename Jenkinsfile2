pipeline {
    agent none {
        stages{
            stage('maven commands'){
                steps{
                    sh 'mv clean compile install package'
                }
            }
        }
    }
}